i want to create split bill app in cloudflare workers using react vite & hono. the feature is user can upload their receipt then AI analyze it and parse it by item, then we arrange people for each item, you also need to consider tax & services in calculations. or if user doesn't have receipt, just let them insert. the AI will use openrouter using gemini. design with neobrutalism with blue color. architecture of app are using MVC pattern.