// Curated list for the Bill editor's currency selector. Any ISO 4217 code
// works through the domain layer — this is a convenience shortlist, not a
// domain constraint.
export const SUPPORTED_CURRENCIES = ['IDR', 'SGD', 'USD', 'EUR', 'GBP', 'JPY', 'AUD', 'MYR'] as const
